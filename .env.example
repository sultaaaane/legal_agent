# Required
OPENAI_API_KEY=sk-...

# Optional — LangSmith tracing (highly recommended for debugging)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=ls__...
LANGCHAIN_PROJECT=legal-contract-reviewer

# Optional — model overrides
PRIMARY_MODEL=gpt-4o          # used for risk scoring, negotiation
FAST_MODEL=gpt-4o-mini        # used for plain english, classification